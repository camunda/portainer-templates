---
services:
  main:
    command: /usr/local/bin/start-container.sh
    deploy:
      restart_policy:
        condition: on-failure
    image: anonymousproxy:8081/camunda-ci-oracle:12
    ports:
      - 1521/tcp
      - 22/tcp
    restart: on-failure
version: '3'
