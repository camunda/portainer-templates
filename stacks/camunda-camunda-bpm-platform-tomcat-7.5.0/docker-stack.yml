---
services:
  main:
    deploy:
      restart_policy:
        condition: on-failure
    environment: []
    image: camunda/camunda-bpm-platform:tomcat-7.5.0
    ports:
      - 8080/tcp
    restart: on-failure
version: '3'
