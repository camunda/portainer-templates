---
services:
  main:
    command: /usr/local/bin/start-container.sh
    deploy:
      restart_policy:
        condition: on-failure
    image: registry.camunda.com/camunda-ci-db2:10.1
    ports:
      - 50000/tcp
      - 22/tcp
    privileged: true
    restart: on-failure
version: '3'
