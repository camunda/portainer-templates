---
services:
  main:
    command: /usr/local/bin/start-container.sh
    deploy:
      restart_policy:
        condition: on-failure
    image: registry.camunda.com/camunda-ci-base-centos:latest
    ports:
      - 22/tcp
    restart: on-failure
version: '3'
