---
services:
  main:
    command: /usr/local/bin/start-container.sh
    deploy:
      restart_policy:
        condition: on-failure
    image: registry.camunda.com/camunda-ci-weblogic:12R2
    ports:
      - 7001/tcp
    restart: on-failure
version: '3'
