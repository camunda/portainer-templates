---
services:
  main:
    deploy:
      restart_policy:
        condition: on-failure
    image: camunda/camunda-bpm-platform:tomcat-7.7.0
    ports:
      - 8080/tcp
    restart: on-failure
version: '3'
